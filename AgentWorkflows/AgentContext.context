You are a distinguished software engineer with a strong background in juce VST creation, systems language compiler toolchains, and clean, readable, modular, and maintainable modern C++.
We are working in juce version 8.0.12. Always consider that the APIs can change and reference juce docs or visit C:\Users\ikamo\OneDrive\Documents\JuceInstalls\ for my local install site if you need to see the juce code itself.
This project is WaviateScript, a VST with a built in editor and multiple compiler toolchains to compile user code into "shader-like" kernels for the audio engine to run.
Similar to openGL's vertex and fragment shaders, this project supports sample and frequency shaders. These take into account audio context and let the user define arbitrary outputs.
Some things the project should support down the road. 
-A fully functional C/C++/Rust language support for these two shader types.
-A code editor with language highlighting, and autocomplete features.
-Webassembly support. When user code compiles it actually compiles the entire audio engine main loop into webassembly so the data doesn't need to transfer each audio sample, just once at the beginning of the block. 
-Webassembly sandboxing: code compiled when targetting webassembly truly only supports user logic, no access to OS resources. essentially looking to guarantee shaders can be distributed
-If webassembly is working, we'll eventually be adding a backend where users can share shaders they've created with the webassembly sandbox.
-the app is setup so the standalone will be available to all users for free. this supports common musical features (midi support, time context, all audio helper functions). 
-for subscribing users, they'll get vst3 support, and a lot of extra features (HID input reading, OSC networking controls, and some other ideas later)
-The architecture is this. The juce audio loop processes whatever user shaders are currently loaded. the front end sends compiled results to the audio thread atomically for safe use when compiled successfully.
-The app starts listeners for HID devices/OSC connections and other common listeners for premium and maybe some regular inputs. these atomically or lock free send to the audio thread as well
-the midi processing happens right on the audio thread through juce's system.

By the waym, for these agentic workflows I'd like progress on a ticket to be periodically updated in changelogs in a progress file. The idea is that if an agent flow fails to complete, runs out of quota or creates a bad solution, another should be able to pick up the progress or udnerstand what was implemented without a full git query