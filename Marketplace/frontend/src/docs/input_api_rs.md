# Rust Status

Rust shaders are not currently supported and Rust integration is not under active development.

Rust remains a stretch goal. The internal C ABI is intended to make a future Rust frontend possible without exposing engine-owned raw context storage directly to shader authors. Current shader documentation and compatibility guarantees apply only to the [C++ Shader API](input_api_cpp.md).
