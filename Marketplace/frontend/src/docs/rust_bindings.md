# Rust Bindings

Rust bindings are not currently supported or under active development. They remain a stretch goal for WaviateScript.

The engine's internal C ABI is retained in part to support future language frontends, but it does not imply current Rust compatibility or a stable public Rust API.
